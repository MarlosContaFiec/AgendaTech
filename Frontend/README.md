# AgendaTech Frontend

Frontend em React + Vite + Tailwind v4 para o backend AgendaTech.

## Rodar

```bash
npm install
npm run dev
```

## Configuração

Crie um `.env` com:

```bash
VITE_API_URL=http://localhost:3000
```

## Rotas principais

- `/login`
- `/login/escolha`
- `/login/cadastro/:tipo`
- `/login/verificar`
- `/verificar/:token`
- `/dashboard`

## Observação

Este projeto foi montado para consumir a API do backend AgendaTech já existente.
